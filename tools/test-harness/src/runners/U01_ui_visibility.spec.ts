// AUTO-GENERATED FROM docs/testing/ui-inventory.json
// DO NOT EDIT — regenerate with `npm run gen:render-specs`
//
// PLACEHOLDER. The committed v6 inventory is invalid under the v7
// fingerprint cutover. A fresh walk against a signed-in renderer is
// required before this file can be regenerated. Until then this
// stub exists so `tsc` passes and the suite skips cleanly with a
// clear reason.

import { test } from '@playwright/test';

test.describe('U01 — UI visibility sweep (auto-generated)', () => {
	test.skip('v7 cutover — re-walk required before U01 can run', () => {
		// Run `npm run explore:walk` against a signed-in host, then
		// `npm run gen:render-specs` to regenerate this file with
		// v7-shaped fingerprints. See docs/testing/fingerprint-v7-plan.md.
	});
});
